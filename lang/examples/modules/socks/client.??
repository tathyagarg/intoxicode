@all
@import("socket")
@import("fs")

fd = socket ~ socket(socket ~ AF_INET, socket ~ SOCK_STREAM, 0)
socket ~ connect(fd, "127.0.0.1", 8000)

fs ~ write(fd, "POST /")

cb = "_"
res = ""

fs ~ read(fd, res, 100)

scream(res, "\n")

fs ~ close(fd)
