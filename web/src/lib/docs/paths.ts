type Path = {
  name: string;
  icon: string;
};

const paths: Record<string, Path> = {
  'home': {
    name: 'Home',
    icon: 'home'
  },
  'first': {
    name: 'First',
    icon: 'first'
  }
}

export default paths;
